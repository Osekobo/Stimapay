services:
  stimapay:
    build: .
    image: stimapay:latest
    container_name: stimapay
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      DATA_DIR: /app/data
    volumes:
      - stimapay-data:/app/data

volumes:
  stimapay-data: